apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "p46-opa-dev.fullname" . }}-test-connection"
  labels:
    {{- include "p46-opa-dev.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "p46-opa-dev.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
