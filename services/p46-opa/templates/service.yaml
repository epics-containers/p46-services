apiVersion: v1
kind: Service
metadata:
  name: {{ include "p46-opa.fullname" . }}
  labels:
    {{- include "p46-opa.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "p46-opa.selectorLabels" . | nindent 4 }}
