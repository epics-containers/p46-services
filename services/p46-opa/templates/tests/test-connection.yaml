apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "p46-opa.fullname" . }}-test-connection"
  labels:
    {{- include "p46-opa.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "p46-opa.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
